name: Build demo runs for docs Space

on:
  workflow_run:
    workflows: ["Release"]
    types: [completed]
  workflow_dispatch:

jobs:
  build:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync --all-groups

      # `nebo deploy --from-source` builds a wheel from the current
      # checkout via `uv build`. Hatch's `artifacts = ["nebo/server/static/**"]`
      # only ships files that exist on disk at build time, and the static
      # bundle is gitignored (it's a generated artifact). Without these
      # two steps the wheel ships an empty `nebo/server/static/`, the
      # daemon's `app.mount("/", StaticFiles(...))` is skipped, and the
      # Space serves `{"detail":"Not Found"}` for every UI route.
      - name: Build UI
        run: |
          cd ui
          npm ci
          npm run build

      - name: Copy UI assets to static directory
        run: |
          rm -rf nebo/server/static/*
          cp -r ui/dist/* nebo/server/static/

      # Publish BEFORE redeploying. The dataset is the durable source of
      # truth, so the Space's fresh container should come up with the new
      # runs already in place — and the redeploy doubles as a cache reset,
      # which is what keeps a rebuilt run from stacking onto the previous
      # version of itself (the daemon has no run deletion).
      - name: Build and publish demo runs to the dataset
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          NEBO_DEMOS_DATASET: rsamf/nebo-demo-runs
        run: uv run python docs/scripts/build_docs_demos.py

      - name: Redeploy demos Space (factory reset)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          NEBO_DEMOS_TOKEN: ${{ secrets.NEBO_DEMOS_TOKEN }}
        run: |
          uv run nebo deploy \
            --space-id rsamf/nebo-demos \
            --api-token "$NEBO_DEMOS_TOKEN" \
            --logdir hf://datasets/rsamf/nebo-demo-runs \
            --read public --write private \
            --from-source
