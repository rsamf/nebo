FROM python:3.12-slim

${serve_comment}
ENV PYTHONUNBUFFERED=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

${install_block}

# Hugging Face Spaces requires Docker apps to listen on port 7860 by
# default. We bind to 0.0.0.0 so the routed traffic reaches us.
EXPOSE 7860

CMD [${serve_args}]
